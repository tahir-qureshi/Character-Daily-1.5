<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/linearlayout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@drawable/progressdialogerror"
    android:orientation="vertical">


    <TextView
        android:id="@+id/tvpoptitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Account exist but password is \n                  incorrect"
        android:textColor="@color/progress_error_text"
        android:textSize="18dp" />

    <TextView
        android:id="@+id/textView11"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="40dp"
        android:clickable="true"
        android:onClick="onClickedTry"
        android:text="Try Again"
        android:textColor="@color/progress_text_try"
        android:textSize="20dp" />

    <TextView
        android:id="@+id/textView111"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="bottom"
        android:layout_marginTop="30dp"
        android:clickable="true"
        android:gravity="bottom"
        android:onClick="onClickedSendMail"
        android:text="Send a password-reset email"
        android:textColor="@color/progress_text_email"
        android:textSize="20dp" />

</LinearLayout>
